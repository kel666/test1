(function() {

	FuncUnit = {
		// the list of browsers that selenium runs tests on
		browsers : [ "*iexplore" ],
		// the number of milliseconds between Selenium commands, "slow" is 500
		// ms
		speed : null
	// "slow"
	}

});